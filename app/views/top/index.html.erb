<h1>食材から同等の栄養価を持つ食材を検索</h1>
<%= render 'layouts/search_form' %>
<table>
  <% if @foods.nil? %>
    <tr><td>検索結果: なし</td></tr>
  <% else %>
    <% @foods.each do |food| %>
      <tr>
        <td>
          <% if food.picture.present? %>
            <%= image_tag(image_path(food.picture), height: '100px') %>
          <% end %>
        </td>
        <td><%= link_to food.name, food %></td>
        <td>protein: <%= food.protein %></td>
        <td>fat: <%= food.fat %></td>
        <td>carbohydrate: <%= food.carbohydrate %></td>
      </tr>
    <% end %>
  <% end %>
</table>